{
  "name": "heroes-of-medicine-static",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0 <21"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "audit:content": "node scripts/audit-content.mjs",
    "audit:videos": "node scripts/audit-videos.mjs",
    "audit:links": "node scripts/audit-links.mjs",
    "test:smoke": "playwright test",
    "test": "npm run build && npm run audit:content && npm run audit:videos && npm run audit:links && npm run test:smoke"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@fontsource/bebas-neue": "^5.2.7",
    "@fontsource/jost": "^5.2.8",
    "@fontsource/playfair-display": "^5.2.8",
    "astro": "^5.18.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "cheerio": "^1.1.2"
  }
}
